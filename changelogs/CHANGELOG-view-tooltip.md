# Fix: Missing View-Mode Tooltips

## Changes
- Added missing `title` attributes to header view-mode buttons (Split, Preview, PPT, Page)
- These buttons had no `title`, causing empty white tooltip boxes on hover

## Files Modified
- `index.html` — added `title` to 4 view-mode buttons in the header center section
