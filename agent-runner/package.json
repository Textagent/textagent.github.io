{
  "name": "agent-runner",
  "version": "1.0.0",
  "private": true,
  "description": "TextAgent cloud exec API — ephemeral Codespace for running external agents",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "bash scripts/setup.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
